redactions = [
  "CONFLUENCE_PAT",
  "GOOGLE_API_KEY",
  "OPENROUTER_API_KEY"
]

[[env._.file]]
  path = "config.env.yaml"

[settings]
  experimental = true
  task.timings = true
  verbose = false

[tools]
  jq = "latest"
  pandoc = "latest"
  "conda:htmd" = "latest"
  "conda:spider_cli" = { version = "latest", depends = ["conda:htmd"] }
  "conda:xee-rust" = { version = "latest", depends = ["conda:spider_cli"] }
  "github:block/goose" = "latest"
